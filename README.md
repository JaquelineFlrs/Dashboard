# Sprint Planner — Server (Full)

Servidor Node/Express con endpoints /api/* y frontend completo en /web. No se exponen URL ni keys en el cliente.

## Setup
npm install
# copia .env.example a .env y completa SUPABASE_URL y SUPABASE_SERVICE_ROLE
npm run dev
